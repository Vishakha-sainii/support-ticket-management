# Database configuration (no secrets committed — set values in your environment)
DATABASE_URL=jdbc:postgresql://localhost:5432/support_tickets
DATABASE_USERNAME=support_user
DATABASE_PASSWORD=

# Demo authentication (development only — see README.md)
# DEMO_ADMIN_PASSWORD=admin123
# DEMO_USER_PASSWORD=user123
